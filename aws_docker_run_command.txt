# for same strategy 

docker run --rm ^ -v %cd%\data:/app/data ^ -v %cd%\results:/app/results ^ backtesting_engine 

# for the different strategies in the .yaml file

:-- if exp.yaml is changed and some new strategy is applied

  docker run --rm ^
  -v %cd%\data:/app/data ^
  -v %cd%\results:/app/results ^
  -v %cd%\config:/app/config ^
  backtesting_engine

# for the cleaning the results every time and displaying the results for the new strategy implemented

docker run --rm ^
  -v %cd%\data:/app/data ^
  -v %cd%\results:/app/results ^
  backtesting_engine ^
  python tools/generate_report.py

# for syncing data thats collected live each minute form aws from the s3 bucket command 

aws s3 sync s3://project-backtesting-data/raw/ ./data/raw/

# for rebuilding image forcefully with no previous data  

docker build --no-cache -t backtesting_engine .